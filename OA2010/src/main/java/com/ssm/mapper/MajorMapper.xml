<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssm.mapper.MajorMapper">

    <select id="getMajors" resultType="Major">
        select id,major_name as 'majorName' from t_major where major_delete=0
     </select>
</mapper>